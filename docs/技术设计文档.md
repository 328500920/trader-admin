# 交易员成长平台 - 技术设计文档

> 项目名称：trader-growth
> 版本：V1.0
> 更新日期：2024年12月

---

## 一、项目概述

### 1.1 项目定位
一个面向数字货币交易员的学习成长平台，提供系统化学习、交易记录、绩效分析和社区交流功能。

### 1.2 技术栈

| 层级 | 技术选型 |
|-----|---------|
| 前端框架 | Vue 3 + Vite |
| UI组件库 | Element Plus |
| 状态管理 | Pinia |
| 路由 | Vue Router 4 |
| HTTP请求 | Axios |
| 图表 | ECharts |
| Markdown | md-editor-v3 |
| 后端框架 | Spring Boot 2.7.x |
| JDK版本 | 1.8 |
| 数据库 | MySQL 5.7 |
| ORM | MyBatis Plus |
| 权限 | Spring Security + JWT |
| 文件存储 | 本地存储 |
| 部署 | Nginx + Jar + MySQL |

### 1.3 项目结构

```
trader-growth/
├── docs/                    # 项目文档
├── sql/                     # 数据库脚本
├── trader-admin/            # 后端项目
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/
│   │   │   │   └── com/trader/
│   │   │   │       ├── common/       # 通用模块
│   │   │   │       ├── config/       # 配置类
│   │   │   │       ├── controller/   # 控制器
│   │   │   │       ├── entity/       # 实体类
│   │   │   │       ├── mapper/       # Mapper接口
│   │   │   │       ├── service/      # 服务层
│   │   │   │       ├── security/     # 安全模块
│   │   │   │       └── utils/        # 工具类
│   │   │   └── resources/
│   │   │       ├── mapper/           # XML映射文件
│   │   │       └── application.yml
│   │   └── test/
│   └── pom.xml
├── trader-web/              # 前端项目
│   ├── src/
│   │   ├── api/             # API接口
│   │   ├── assets/          # 静态资源
│   │   ├── components/      # 公共组件
│   │   ├── layout/          # 布局组件
│   │   ├── router/          # 路由配置
│   │   ├── store/           # 状态管理
│   │   ├── styles/          # 样式文件
│   │   ├── utils/           # 工具函数
│   │   └── views/           # 页面组件
│   ├── index.html
│   ├── vite.config.js
│   └── package.json
└── README.md
```

---

## 二、功能模块设计

### 2.1 模块总览

```
┌─────────────────────────────────────────────────────────┐
│                      系统功能架构                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │ 用户管理 │ │ 学习中心 │ │ 交易日志 │ │ 绩效分析 │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
│                                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │ 社区交流 │ │ 工具箱   │ │ 系统管理 │ │ 个人中心 │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 2.2 各模块功能详情

#### 模块1：用户管理
```
- 用户登录（JWT Token）
- 用户信息管理
- 密码修改
- 管理员：创建用户、禁用用户、重置密码
```

#### 模块2：学习中心
```
- 课程分类管理（阶段/周）
- 课程内容管理（Markdown编辑器）
- 学习进度追踪
- 任务清单（可勾选）
- 学习笔记
```

#### 模块3：交易日志
```
- 新建交易记录
- 交易列表（筛选、搜索）
- 交易详情
- 上传截图（K线图）
- 交易复盘
```

#### 模块4：绩效分析
```
- 总体统计（胜率、盈亏比、总收益）
- 收益曲线图
- 月度/周度对比
- 按策略分析
- 按品种分析
```

#### 模块5：社区交流
```
- 帖子发布（Markdown）
- 帖子列表
- 评论功能
- 点赞功能
- 帖子分类（学习心得、交易分享、问答）
```

#### 模块6：工具箱
```
- 交易前检查清单
- 每日分析模板
- 常用链接导航
- 仓位计算器
- 盈亏比计算器
```

#### 模块7：系统管理（管理员）
```
- 用户管理
- 课程管理
- 帖子审核
- 系统配置
```

---

## 三、数据库设计

### 3.1 ER图概览

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   sys_user  │     │ sys_role    │     │ sys_menu    │
│   用户表    │────→│ 角色表      │────→│ 菜单表      │
└─────────────┘     └─────────────┘     └─────────────┘
       │
       │
       ↓
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│learn_course │     │learn_chapter│     │learn_progress│
│  课程表     │────→│  章节表     │←────│  进度表     │
└─────────────┘     └─────────────┘     └─────────────┘
       
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│ trade_log   │     │ trade_image │     │trade_summary│
│ 交易日志表  │────→│ 交易截图表  │     │ 绩效汇总表  │
└─────────────┘     └─────────────┘     └─────────────┘

┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│ community_  │     │ community_  │     │ community_  │
│ post 帖子表 │────→│ comment评论 │     │ like 点赞   │
└─────────────┘     └─────────────┘     └─────────────┘
```

### 3.2 表结构设计

#### 3.2.1 用户相关表

```sql
-- 用户表
CREATE TABLE sys_user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID',
    username VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
    password VARCHAR(100) NOT NULL COMMENT '密码',
    nickname VARCHAR(50) COMMENT '昵称',
    avatar VARCHAR(255) COMMENT '头像URL',
    email VARCHAR(100) COMMENT '邮箱',
    phone VARCHAR(20) COMMENT '手机号',
    role_id BIGINT NOT NULL COMMENT '角色ID',
    status TINYINT DEFAULT 1 COMMENT '状态：0禁用 1启用',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    last_login_time DATETIME COMMENT '最后登录时间',
    INDEX idx_username (username),
    INDEX idx_role_id (role_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 角色表
CREATE TABLE sys_role (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '角色ID',
    role_name VARCHAR(50) NOT NULL COMMENT '角色名称',
    role_code VARCHAR(50) NOT NULL UNIQUE COMMENT '角色编码',
    description VARCHAR(255) COMMENT '描述',
    status TINYINT DEFAULT 1 COMMENT '状态：0禁用 1启用',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色表';

-- 菜单表
CREATE TABLE sys_menu (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '菜单ID',
    parent_id BIGINT DEFAULT 0 COMMENT '父菜单ID',
    menu_name VARCHAR(50) NOT NULL COMMENT '菜单名称',
    menu_type TINYINT COMMENT '类型：1目录 2菜单 3按钮',
    path VARCHAR(200) COMMENT '路由路径',
    component VARCHAR(200) COMMENT '组件路径',
    icon VARCHAR(100) COMMENT '图标',
    sort_order INT DEFAULT 0 COMMENT '排序',
    visible TINYINT DEFAULT 1 COMMENT '是否可见',
    status TINYINT DEFAULT 1 COMMENT '状态',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='菜单表';

-- 角色菜单关联表
CREATE TABLE sys_role_menu (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    role_id BIGINT NOT NULL COMMENT '角色ID',
    menu_id BIGINT NOT NULL COMMENT '菜单ID',
    UNIQUE KEY uk_role_menu (role_id, menu_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色菜单关联表';
```

#### 3.2.2 学习相关表

```sql
-- 课程表
CREATE TABLE learn_course (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '课程ID',
    title VARCHAR(100) NOT NULL COMMENT '课程标题',
    description TEXT COMMENT '课程描述',
    cover_image VARCHAR(255) COMMENT '封面图片',
    stage INT NOT NULL COMMENT '阶段：1/2/3',
    sort_order INT DEFAULT 0 COMMENT '排序',
    status TINYINT DEFAULT 1 COMMENT '状态：0草稿 1发布',
    create_by BIGINT COMMENT '创建人',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_stage (stage)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='课程表';

-- 章节表
CREATE TABLE learn_chapter (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '章节ID',
    course_id BIGINT NOT NULL COMMENT '课程ID',
    title VARCHAR(100) NOT NULL COMMENT '章节标题',
    content LONGTEXT COMMENT '章节内容（Markdown）',
    week_number INT COMMENT '周数',
    sort_order INT DEFAULT 0 COMMENT '排序',
    status TINYINT DEFAULT 1 COMMENT '状态',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_course_id (course_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='章节表';

-- 学习任务表
CREATE TABLE learn_task (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '任务ID',
    chapter_id BIGINT NOT NULL COMMENT '章节ID',
    task_content VARCHAR(500) NOT NULL COMMENT '任务内容',
    sort_order INT DEFAULT 0 COMMENT '排序',
    INDEX idx_chapter_id (chapter_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学习任务表';

-- 学习进度表
CREATE TABLE learn_progress (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT 'ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    chapter_id BIGINT NOT NULL COMMENT '章节ID',
    is_completed TINYINT DEFAULT 0 COMMENT '是否完成',
    complete_time DATETIME COMMENT '完成时间',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    UNIQUE KEY uk_user_chapter (user_id, chapter_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学习进度表';

-- 任务完成记录表
CREATE TABLE learn_task_record (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT 'ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    task_id BIGINT NOT NULL COMMENT '任务ID',
    is_completed TINYINT DEFAULT 0 COMMENT '是否完成',
    complete_time DATETIME COMMENT '完成时间',
    UNIQUE KEY uk_user_task (user_id, task_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='任务完成记录表';

-- 学习笔记表
CREATE TABLE learn_note (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '笔记ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    chapter_id BIGINT COMMENT '关联章节ID',
    title VARCHAR(100) COMMENT '笔记标题',
    content LONGTEXT COMMENT '笔记内容',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_user_id (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学习笔记表';
```

#### 3.2.3 交易相关表

```sql
-- 交易日志表
CREATE TABLE trade_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '交易ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    trade_date DATE NOT NULL COMMENT '交易日期',
    symbol VARCHAR(20) NOT NULL COMMENT '交易品种（BTC/ETH）',
    direction TINYINT NOT NULL COMMENT '方向：1做多 2做空',
    strategy VARCHAR(50) COMMENT '使用策略',
    
    -- 入场信息
    entry_price DECIMAL(20,8) NOT NULL COMMENT '入场价格',
    entry_time DATETIME COMMENT '入场时间',
    entry_reason TEXT COMMENT '入场理由',
    
    -- 出场信息
    exit_price DECIMAL(20,8) COMMENT '出场价格',
    exit_time DATETIME COMMENT '出场时间',
    exit_reason VARCHAR(50) COMMENT '出场原因：止盈/止损/手动',
    
    -- 计划
    stop_loss DECIMAL(20,8) COMMENT '止损价',
    take_profit DECIMAL(20,8) COMMENT '止盈价',
    position_size DECIMAL(20,8) COMMENT '仓位大小(USDT)',
    
    -- 结果
    profit_loss DECIMAL(20,8) COMMENT '盈亏金额',
    profit_loss_percent DECIMAL(10,4) COMMENT '盈亏百分比',
    status TINYINT DEFAULT 0 COMMENT '状态：0持仓中 1已平仓',
    
    -- 分析
    macro_analysis TEXT COMMENT '宏观分析',
    chain_analysis TEXT COMMENT '链上分析',
    sentiment_analysis TEXT COMMENT '情绪分析',
    technical_analysis TEXT COMMENT '技术分析',
    
    -- 复盘
    review TEXT COMMENT '交易复盘',
    emotion_score INT COMMENT '情绪评分1-10',
    discipline_followed TINYINT COMMENT '是否遵守纪律',
    
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    
    INDEX idx_user_id (user_id),
    INDEX idx_trade_date (trade_date),
    INDEX idx_symbol (symbol)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='交易日志表';

-- 交易截图表
CREATE TABLE trade_image (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT 'ID',
    trade_id BIGINT NOT NULL COMMENT '交易ID',
    image_url VARCHAR(255) NOT NULL COMMENT '图片URL',
    image_type VARCHAR(20) COMMENT '图片类型：entry/exit/analysis',
    description VARCHAR(255) COMMENT '图片描述',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    INDEX idx_trade_id (trade_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='交易截图表';

-- 绩效汇总表（按月统计）
CREATE TABLE trade_summary (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT 'ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    summary_month VARCHAR(7) NOT NULL COMMENT '统计月份（2024-01）',
    
    total_trades INT DEFAULT 0 COMMENT '总交易次数',
    win_trades INT DEFAULT 0 COMMENT '盈利次数',
    lose_trades INT DEFAULT 0 COMMENT '亏损次数',
    win_rate DECIMAL(5,2) COMMENT '胜率',
    
    total_profit DECIMAL(20,8) DEFAULT 0 COMMENT '总盈利',
    total_loss DECIMAL(20,8) DEFAULT 0 COMMENT '总亏损',
    net_profit DECIMAL(20,8) DEFAULT 0 COMMENT '净盈亏',
    profit_factor DECIMAL(10,4) COMMENT '盈亏比',
    
    max_profit DECIMAL(20,8) COMMENT '最大单笔盈利',
    max_loss DECIMAL(20,8) COMMENT '最大单笔亏损',
    max_drawdown DECIMAL(10,4) COMMENT '最大回撤',
    
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    
    UNIQUE KEY uk_user_month (user_id, summary_month)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='绩效汇总表';
```

#### 3.2.4 社区相关表

```sql
-- 帖子表
CREATE TABLE community_post (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '帖子ID',
    user_id BIGINT NOT NULL COMMENT '作者ID',
    title VARCHAR(200) NOT NULL COMMENT '标题',
    content LONGTEXT NOT NULL COMMENT '内容（Markdown）',
    category VARCHAR(50) COMMENT '分类：学习心得/交易分享/问答',
    view_count INT DEFAULT 0 COMMENT '浏览量',
    like_count INT DEFAULT 0 COMMENT '点赞数',
    comment_count INT DEFAULT 0 COMMENT '评论数',
    is_top TINYINT DEFAULT 0 COMMENT '是否置顶',
    status TINYINT DEFAULT 1 COMMENT '状态：0草稿 1发布 2审核中 3已删除',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_user_id (user_id),
    INDEX idx_category (category),
    INDEX idx_create_time (create_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='帖子表';

-- 评论表
CREATE TABLE community_comment (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '评论ID',
    post_id BIGINT NOT NULL COMMENT '帖子ID',
    user_id BIGINT NOT NULL COMMENT '评论者ID',
    parent_id BIGINT DEFAULT 0 COMMENT '父评论ID（回复）',
    content TEXT NOT NULL COMMENT '评论内容',
    like_count INT DEFAULT 0 COMMENT '点赞数',
    status TINYINT DEFAULT 1 COMMENT '状态',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    INDEX idx_post_id (post_id),
    INDEX idx_user_id (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评论表';

-- 点赞表
CREATE TABLE community_like (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT 'ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    target_type TINYINT NOT NULL COMMENT '目标类型：1帖子 2评论',
    target_id BIGINT NOT NULL COMMENT '目标ID',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    UNIQUE KEY uk_user_target (user_id, target_type, target_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='点赞表';
```

#### 3.2.5 工具相关表

```sql
-- 检查清单模板表
CREATE TABLE tool_checklist (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT 'ID',
    name VARCHAR(100) NOT NULL COMMENT '清单名称',
    description VARCHAR(255) COMMENT '描述',
    items TEXT COMMENT '清单项（JSON数组）',
    is_system TINYINT DEFAULT 0 COMMENT '是否系统预设',
    create_by BIGINT COMMENT '创建人',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='检查清单模板表';

-- 常用链接表
CREATE TABLE tool_link (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT 'ID',
    name VARCHAR(100) NOT NULL COMMENT '链接名称',
    url VARCHAR(500) NOT NULL COMMENT '链接地址',
    icon VARCHAR(100) COMMENT '图标',
    category VARCHAR(50) COMMENT '分类',
    sort_order INT DEFAULT 0 COMMENT '排序',
    status TINYINT DEFAULT 1 COMMENT '状态'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='常用链接表';

-- 每日分析记录表
CREATE TABLE tool_daily_analysis (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT 'ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    analysis_date DATE NOT NULL COMMENT '分析日期',
    
    -- 宏观环境
    macro_events TEXT COMMENT '今日重要事件',
    fed_attitude VARCHAR(20) COMMENT '美联储态度',
    dxy_trend VARCHAR(20) COMMENT 'DXY趋势',
    stock_trend VARCHAR(20) COMMENT '美股走势',
    
    -- 链上数据
    exchange_flow VARCHAR(20) COMMENT '交易所流向',
    whale_action VARCHAR(50) COMMENT '巨鲸动向',
    
    -- 市场情绪
    fear_greed_index INT COMMENT '恐惧贪婪指数',
    funding_rate DECIMAL(10,4) COMMENT '资金费率',
    long_short_ratio DECIMAL(10,4) COMMENT '多空比',
    
    -- 技术分析
    btc_price DECIMAL(20,8) COMMENT 'BTC价格',
    weekly_trend VARCHAR(20) COMMENT '周线趋势',
    daily_trend VARCHAR(20) COMMENT '日线趋势',
    key_support DECIMAL(20,8) COMMENT '关键支撑',
    key_resistance DECIMAL(20,8) COMMENT '关键阻力',
    
    -- 综合判断
    overall_score INT COMMENT '综合评分',
    market_view VARCHAR(50) COMMENT '市场观点',
    today_strategy VARCHAR(50) COMMENT '今日策略',
    notes TEXT COMMENT '备注',
    
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    UNIQUE KEY uk_user_date (user_id, analysis_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='每日分析记录表';
```

---

## 四、API接口设计

### 4.1 接口规范

**请求格式**
```
基础URL: /api
Content-Type: application/json
Authorization: Bearer {token}
```

**响应格式**
```json
{
    "code": 200,
    "message": "success",
    "data": {}
}
```

**状态码**
| 状态码 | 说明 |
|-------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未登录/Token过期 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器错误 |

### 4.2 接口列表

#### 4.2.1 认证模块 `/api/auth`

| 方法 | 路径 | 说明 |
|-----|------|------|
| POST | /login | 用户登录 |
| POST | /logout | 退出登录 |
| GET | /info | 获取当前用户信息 |
| PUT | /password | 修改密码 |

#### 4.2.2 用户管理 `/api/user`（管理员）

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /list | 用户列表 |
| POST | / | 创建用户 |
| PUT | /{id} | 更新用户 |
| DELETE | /{id} | 删除用户 |
| PUT | /{id}/status | 启用/禁用用户 |
| PUT | /{id}/reset-password | 重置密码 |

#### 4.2.3 学习模块 `/api/learn`

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /course/list | 课程列表 |
| GET | /course/{id} | 课程详情 |
| POST | /course | 创建课程（管理员） |
| PUT | /course/{id} | 更新课程（管理员） |
| DELETE | /course/{id} | 删除课程（管理员） |
| GET | /chapter/list | 章节列表 |
| GET | /chapter/{id} | 章节详情 |
| POST | /chapter | 创建章节（管理员） |
| PUT | /chapter/{id} | 更新章节（管理员） |
| GET | /progress | 获取学习进度 |
| POST | /progress/complete | 标记章节完成 |
| GET | /task/list | 任务列表 |
| POST | /task/complete | 标记任务完成 |
| GET | /note/list | 笔记列表 |
| POST | /note | 创建笔记 |
| PUT | /note/{id} | 更新笔记 |
| DELETE | /note/{id} | 删除笔记 |

#### 4.2.4 交易模块 `/api/trade`

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /list | 交易列表 |
| GET | /{id} | 交易详情 |
| POST | / | 创建交易记录 |
| PUT | /{id} | 更新交易记录 |
| DELETE | /{id} | 删除交易记录 |
| PUT | /{id}/close | 平仓 |
| POST | /{id}/image | 上传截图 |
| DELETE | /image/{id} | 删除截图 |
| GET | /summary | 绩效汇总 |
| GET | /summary/chart | 收益曲线数据 |
| GET | /summary/strategy | 按策略统计 |
| GET | /summary/symbol | 按品种统计 |

#### 4.2.5 社区模块 `/api/community`

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /post/list | 帖子列表 |
| GET | /post/{id} | 帖子详情 |
| POST | /post | 发布帖子 |
| PUT | /post/{id} | 更新帖子 |
| DELETE | /post/{id} | 删除帖子 |
| POST | /post/{id}/like | 点赞帖子 |
| GET | /comment/list | 评论列表 |
| POST | /comment | 发表评论 |
| DELETE | /comment/{id} | 删除评论 |
| POST | /comment/{id}/like | 点赞评论 |

#### 4.2.6 工具模块 `/api/tool`

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /checklist/list | 检查清单列表 |
| GET | /checklist/{id} | 检查清单详情 |
| POST | /checklist | 创建检查清单 |
| GET | /link/list | 常用链接列表 |
| POST | /link | 添加链接（管理员） |
| GET | /analysis/list | 每日分析列表 |
| GET | /analysis/{date} | 获取某日分析 |
| POST | /analysis | 保存每日分析 |

#### 4.2.7 文件上传 `/api/file`

| 方法 | 路径 | 说明 |
|-----|------|------|
| POST | /upload | 上传文件 |
| POST | /upload/image | 上传图片 |

---

## 五、前端页面设计

### 5.1 页面结构

```
├── 登录页 /login
├── 首页（仪表盘） /dashboard
├── 学习中心 /learn
│   ├── 课程列表 /learn/course
│   ├── 课程详情 /learn/course/:id
│   ├── 章节学习 /learn/chapter/:id
│   ├── 我的笔记 /learn/note
│   └── 学习进度 /learn/progress
├── 交易日志 /trade
│   ├── 交易列表 /trade/list
│   ├── 新建交易 /trade/create
│   ├── 交易详情 /trade/detail/:id
│   └── 绩效分析 /trade/analysis
├── 社区 /community
│   ├── 帖子列表 /community/list
│   ├── 帖子详情 /community/post/:id
│   └── 发布帖子 /community/create
├── 工具箱 /tool
│   ├── 检查清单 /tool/checklist
│   ├── 每日分析 /tool/analysis
│   ├── 常用链接 /tool/link
│   └── 计算器 /tool/calculator
├── 个人中心 /profile
│   ├── 个人信息 /profile/info
│   └── 修改密码 /profile/password
└── 系统管理 /system（管理员）
    ├── 用户管理 /system/user
    ├── 课程管理 /system/course
    └── 帖子管理 /system/post
```

### 5.2 页面布局

```
┌─────────────────────────────────────────────────────────┐
│                      顶部导航栏                          │
│  Logo    首页  学习  交易  社区  工具    [主题] [用户]   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│                                                         │
│                      主内容区域                          │
│                                                         │
│                                                         │
│                                                         │
│                                                         │
└─────────────────────────────────────────────────────────┘

移动端：
┌─────────────────┐
│    顶部导航     │
├─────────────────┤
│                 │
│    主内容区域    │
│                 │
├─────────────────┤
│   底部Tab导航   │
└─────────────────┘
```

### 5.3 核心页面设计

#### 5.3.1 首页仪表盘

```
┌─────────────────────────────────────────────────────────┐
│  欢迎回来，{用户名}！                    2024-12-14     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐       │
│  │ 学习进度    │ │ 本月交易    │ │ 本月收益    │       │
│  │   45%      │ │   12笔     │ │  +8.5%     │       │
│  └─────────────┘ └─────────────┘ └─────────────┘       │
│                                                         │
│  ┌─────────────────────────┐ ┌─────────────────────┐   │
│  │ 本周学习任务            │ │ 最近交易            │   │
│  │ □ 完成MACD学习         │ │ BTC 做多 +2.3%     │   │
│  │ ☑ 阅读布林带章节       │ │ ETH 做空 -1.2%     │   │
│  │ □ 完成5笔模拟交易      │ │ BTC 做多 +3.1%     │   │
│  └─────────────────────────┘ └─────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 收益曲线                                        │   │
│  │ [ECharts图表]                                   │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### 5.3.2 学习中心

```
┌─────────────────────────────────────────────────────────┐
│  学习中心                              总进度: 45%      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  阶段一：基础构建期                    [========  ] 80% │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐      │
│  │ 第1周   │ │ 第2周   │ │ 第3周   │ │ 第4周   │      │
│  │ ✓完成   │ │ ✓完成   │ │ 进行中  │ │ 未开始  │      │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘      │
│                                                         │
│  阶段二：策略实战期                    [====      ] 40% │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐      │
│  │ 第9周   │ │ 第10周  │ │ 第11周  │ │ 第12周  │      │
│  │ ✓完成   │ │ 进行中  │ │ 未开始  │ │ 未开始  │      │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘      │
│                                                         │
│  阶段三：进阶稳定期                    [          ] 0%  │
│  ...                                                    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### 5.3.3 交易日志列表

```
┌─────────────────────────────────────────────────────────┐
│  交易日志                              [+ 新建交易]     │
├─────────────────────────────────────────────────────────┤
│  筛选: [全部品种▼] [全部策略▼] [本月▼]    🔍 搜索     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ #12  BTC/USDT  做多  趋势跟随                   │   │
│  │ 入场: $42,350  出场: $43,500  盈亏: +2.7%      │   │
│  │ 2024-12-13                              [详情]  │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ #11  ETH/USDT  做空  情绪反转                   │   │
│  │ 入场: $2,280   出场: $2,350   盈亏: -3.1%      │   │
│  │ 2024-12-12                              [详情]  │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ...                                                    │
│                                                         │
│  [1] [2] [3] ... [10]                                  │
└─────────────────────────────────────────────────────────┘
```

#### 5.3.4 绩效分析

```
┌─────────────────────────────────────────────────────────┐
│  绩效分析                              [本月▼] [导出]   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │ 总交易   │ │ 胜率     │ │ 盈亏比   │ │ 净收益   │  │
│  │  24笔   │ │  45.8%  │ │  2.1:1  │ │ +12.3%  │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 收益曲线                                        │   │
│  │ [ECharts折线图]                                 │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌────────────────────┐ ┌────────────────────┐        │
│  │ 按策略统计         │ │ 按品种统计         │        │
│  │ [ECharts饼图]      │ │ [ECharts饼图]      │        │
│  └────────────────────┘ └────────────────────┘        │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 六、主题切换设计

### 6.1 CSS变量定义

```css
/* 浅色主题 */
:root {
    --bg-primary: #ffffff;
    --bg-secondary: #f5f7fa;
    --text-primary: #303133;
    --text-secondary: #606266;
    --border-color: #dcdfe6;
    --success-color: #67c23a;
    --danger-color: #f56c6c;
    --primary-color: #409eff;
}

/* 深色主题 */
[data-theme="dark"] {
    --bg-primary: #1a1a2e;
    --bg-secondary: #16213e;
    --text-primary: #e0e0e0;
    --text-secondary: #a0a0a0;
    --border-color: #3a3a5a;
    --success-color: #00d4aa;
    --danger-color: #ff6b6b;
    --primary-color: #5c7cfa;
}
```

### 6.2 主题切换实现

```javascript
// store/theme.js
export const useThemeStore = defineStore('theme', {
    state: () => ({
        theme: localStorage.getItem('theme') || 'light'
    }),
    actions: {
        toggleTheme() {
            this.theme = this.theme === 'light' ? 'dark' : 'light';
            document.documentElement.setAttribute('data-theme', this.theme);
            localStorage.setItem('theme', this.theme);
        }
    }
});
```

---

## 七、移动端适配

### 7.1 响应式断点

```css
/* 移动端 */
@media (max-width: 768px) { }

/* 平板 */
@media (min-width: 769px) and (max-width: 1024px) { }

/* 桌面 */
@media (min-width: 1025px) { }
```

### 7.2 移动端特殊处理

- 顶部导航简化，使用汉堡菜单
- 底部添加Tab导航栏
- 表格改为卡片列表展示
- 表单单列布局
- 图表自适应宽度

---

## 八、开发计划

### 8.1 第一期（MVP）- 2周

**后端**
- [ ] 项目初始化、基础配置
- [ ] 用户认证模块（登录、JWT）
- [ ] 用户管理CRUD
- [ ] 学习模块（课程、章节、进度）
- [ ] 交易日志CRUD
- [ ] 文件上传

**前端**
- [ ] 项目初始化、路由配置
- [ ] 登录页面
- [ ] 布局组件（顶部导航、侧边栏）
- [ ] 首页仪表盘
- [ ] 学习中心页面
- [ ] 交易日志页面
- [ ] 主题切换

### 8.2 第二期 - 1周

- [ ] 绩效分析（图表）
- [ ] 社区模块
- [ ] 工具箱（检查清单、每日分析）
- [ ] 移动端适配优化

### 8.3 第三期 - 1周

- [ ] 数据导入（MD文档）
- [ ] 系统管理完善
- [ ] 性能优化
- [ ] 部署上线

---

## 九、部署架构

```
┌─────────────────────────────────────────────────────────┐
│                      服务器                             │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────┐    ┌─────────────┐    ┌───────────┐  │
│  │   Nginx     │    │ Spring Boot │    │  MySQL    │  │
│  │   :80/443   │───→│   :8080     │───→│  :3306    │  │
│  │             │    │             │    │           │  │
│  │ 静态资源    │    │  API服务    │    │  数据库   │  │
│  │ 反向代理    │    │  文件存储   │    │           │  │
│  └─────────────┘    └─────────────┘    └───────────┘  │
│                                                         │
│  /var/www/trader-web/dist/    前端静态文件             │
│  /opt/trader-admin/           后端Jar包                │
│  /opt/trader-admin/uploads/   上传文件目录             │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### Nginx配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态资源
    location / {
        root /var/www/trader-web/dist;
        try_files $uri $uri/ /index.html;
    }

    # API代理
    location /api {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 上传文件访问
    location /uploads {
        alias /opt/trader-admin/uploads;
    }
}
```

---

## 十、项目初始化命令

### 后端初始化

```bash
# 创建Spring Boot项目（使用Spring Initializr或手动创建）
# 依赖：Spring Web, Spring Security, MyBatis Plus, MySQL Driver, Lombok
```

### 前端初始化

```bash
# 创建Vue3项目
npm create vite@latest trader-web -- --template vue

# 安装依赖
cd trader-web
npm install
npm install vue-router@4 pinia axios element-plus @element-plus/icons-vue
npm install echarts md-editor-v3
npm install -D sass
```

---

*文档版本：V1.0*
*创建日期：2024年12月*

**确认无误后，我将开始创建项目代码。**
